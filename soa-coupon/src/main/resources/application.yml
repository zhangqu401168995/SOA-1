spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/soa_sms?serverTimezone=UTC
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: 1
  application:
    name: coupon


mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 7000


